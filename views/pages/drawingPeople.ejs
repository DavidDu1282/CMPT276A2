<!DOCTYPE html>
<html>
<head>
  <% include ../partials/header.ejs %>
</head>

<body>

<% include ../partials/nav.ejs %>

<div class="container">
<h2>Main Display</h2>


    <% i = 0 %>
    <% rows.forEach(function(r) { %>
        <% i+= r.height*10 + 30%>
        <svg width="400" height="110">
          <rect width="<%= r.size/r.height*10 %>" height="<%= r.height*10 %>" style="fill:rgb(0,0,255);stroke-width:3;stroke:rgb(0,0,0)" />

          Sorry, your browser does not support inline SVG.
        </svg>
    <% }); %>


</div>

</body>
</html>
