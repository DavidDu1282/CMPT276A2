<!DOCTYPE html>
<html>
<head>
  <% include ../partials/header.ejs %>
</head>

<body>

<% include ../partials/nav.ejs %>

<div class="container">
<h2>Database Results</h2>

<ul>
    <% rows.forEach(function(r) { %>
        console.log("%s,%d,%d,%s", r.Name, r.Size, r.Height, r.Type)
        <li><%= r.Name %><%= r.Size %><%= r.Height %><%= r.Type %></li>
    <% }); %>
</ul>

</div>

</body>
</html>
